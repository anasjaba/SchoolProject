{% extends "base.twig" %}
{% set active_page = "editor_unassigned" %}

{% block title %}New submissions{% endblock %}

{% block content %}
  <h1 class="page-header">New submissions</h1>
   <ol class="breadcrumb">
    <li><a href="{{ app.url_generator.generate('editor_dashboard') }}">Home</a></li>
    <li class="active">Unassigned</li>
  </ol>

  <!-- Table -->
  <table class="table">

    <thead>
      <tr>
        <th> Edit </th>
        <th> Author </th>
        <th> Title </th>
        <th> Section </th>
        <th> Submission date </th>
      </tr>
    </thead>

    <tr>
      <td style="width: 150px;">
        <a class="btn btn-primary" href="{{ app.url_generator.generate('editor_submission_detail', {id: 1}) }}" role="button" style="font-size:10px;">
          <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
        </a>
      </td>
      <td> <a>Rondeau, S</a> </td>
      <td> <a href="{{ app.url_generator.generate('editor_submission_detail', {id: 1}) }}">The Physics of Stein;Gate</a> </td>
      <td> Physics </td>
      <td> 2015-02-25 </td>
    </tr>

    <tr>
      <td style="width: 150px;">
        <a class="btn btn-primary" href="{{ app.url_generator.generate('editor_submission_detail', {id: 2}) }}" role="button" style="font-size:10px;">
          <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
        </a>
      </td>
      <td> <a>Rondeau, S</a> </td>
      <td> <a href="{{ app.url_generator.generate('editor_submission_detail', {id: 2}) }}">The Physics of Stein;Gate</a> </td>
      <td> Physics </td>
      <td> 2015-02-25 </td>
    </tr>

    <tr>
      <td style="width: 150px;">
        <a class="btn btn-primary" href="{{ app.url_generator.generate('editor_submission_detail', {id: 3}) }}" role="button" style="font-size:10px;">
          <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
        </a>
      </td>
      <td> <a>Rondeau, S</a> </td>
      <td> <a href="{{ app.url_generator.generate('editor_submission_detail', {id: 3}) }}">The Physics of Stein;Gate</a> </td>
      <td> Physics </td>
      <td> 2015-02-25 </td>
    </tr>

  </table>

{% endblock %}